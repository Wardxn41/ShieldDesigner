// Centralized constants used across the app.

// Stamp size means: max dimension in canvas pixels.
export const DEFAULT_STAMP_SIZE = 120;

// Handle sizes are specified in UI pixels; these get converted to canvas units.
export const HANDLE_SIZE_UI_PX = 10;
export const HANDLE_HIT_RADIUS_UI_PX = 18;
export const ROTATE_HIT_RADIUS_UI_PX = 22;
export const ROTATE_HANDLE_DIST_UI_PX = 30;
